export interface IinitState {
    employees: any[],
    chosenEmployee: any,
    layout: number,
    filteredEmployees: any[],
    status: number,
    error: string | null,
    message: string | null,
}