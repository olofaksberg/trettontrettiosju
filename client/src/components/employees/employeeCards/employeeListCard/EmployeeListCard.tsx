/** @format */

import { IemployeeListCardProps } from "./employeeListCard.types";

export const EmployeeListCard = (props: IemployeeListCardProps) => {
 const { employee } = props;
 return <div className={`employee-listcard`}></div>;
};
